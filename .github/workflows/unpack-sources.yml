name: Prepare Arunika sources

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  unpack:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Unpack generated source files
        run: |
          gzip -dc components/ArunikaApp.tsx.gz > components/ArunikaApp.tsx
          gzip -dc components/ArunikaViews.tsx.gz > components/ArunikaViews.tsx
          gzip -dc app/globals.css.gz > app/globals.css
          rm components/ArunikaApp.tsx.gz components/ArunikaViews.tsx.gz app/globals.css.gz
      - name: Commit prepared sources
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Prepare Arunika application sources"
          git push
